/*¿Qué es long int en programación? Las variables de tipo entero grandes o long son aquellas que almacenan 
un número (ya sea positivo o negativo) no decimal . Las variables de tipo long 
almacenan números que están dentro del rango -2^63 y 2^63 - 1 .*/

#include <stdio.h>
long int potencia(int x, int n);                               

int main()
{
int a;
a=potencia(2,3);
printf("%d",a);

}


long int potencia(int x, int n)
{
    long int resultado=x;
    int cantidad;
    for(cantidad=1;cantidad<n;cantidad++){
        resultado=resultado*x;
    }
    return resultado;
}
